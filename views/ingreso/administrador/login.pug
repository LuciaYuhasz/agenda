extends ../../layout.pug

block contenido
  .container
    html(lang="es")
      head
        title Login - Agenda Médica
        link(href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css", rel="stylesheet")
        style.
          body {
            background-color: #f8f9fa;
          }
          .login-container {
            max-width: 400px;
            margin: 50px auto;
            padding: 20px;
            background: white;
            border-radius: 15px;
            box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.2);
          }
      body
        .container
          .login-container
            h1.text-center.mb-4 Login
            form(action="/administrador/login" method="POST")
              .mb-3
                label(for="email" class="form-label") Email:
                input(type="email" class="form-control" name="email" required)
              .mb-3
                label(for="password" class="form-label") Contraseña:
                input(type="password" class="form-control" name="password" required)
              button(type="submit" class="btn btn-primary w-100") Iniciar sesión
            p.text-center.mt-3
              | ¿No tienes cuenta? 
              a(href="/administrador/register" class="text-decoration-none") Regístrate aquí