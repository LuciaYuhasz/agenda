doctype html
html(lang="es")
  head
    title ADMINISTRADOR
    link(href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css", rel="stylesheet")
    link(rel="stylesheet", href="/styles.css")
    style.
      body {
        background-color: #fef6e4;
        font-size: 1.2rem;
        color: #4a4e69;
      }
      .card-container {
        text-align: center;
      }
      .card {
        border: none;
        background-color: #f4a261;
        color: #ffffff;
        border-radius: 8px;  /* Bordes redondeados */
        width: 250px;  /* Tamaño uniforme */
        height: 250px;  /* Ajustamos la altura para que todo encaje bien */
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        margin: 0 auto 20px;
        padding: 20px;
        transition: transform 0.3s, box-shadow 0.3s;
      }
      .card:hover {
        transform: scale(1.05);
        box-shadow: 0px 10px 30px rgba(0, 0, 0, 0.3);
      }
      .btn-custom {
        background-color: #264653;
        border: none;
        color: #fef6e4;
        font-weight: bold;
        text-transform: uppercase;
        padding: 10px 20px;
        font-size: 1rem;
        transition: background-color 0.3s;
        width: 100%;
      }
      .btn-custom:hover {
        background-color: #2a9d8f;
      }
      .dropdown-menu {
        min-width: 250px;
      }
      .card-title {
        margin-bottom: 10px;
      }
      .card-text {
        font-size: 0.9rem;
        margin-top: 10px;
      }
      .card-footer {
        font-size: 0.9rem;
        color: #4a4e69;
        margin-top: 10px;
      }

      /* Estilos para las descripciones fuera de las tarjetas */
      .card-description {
        font-size: 0.9rem;
        margin-top: 10px;
        color: #4a4e69;
        text-align: center;
      }

  body
    .container.my-5
      br
      h1.text-center.mb-5 Bienvenido al Panel de Administración
      br
      br
      br

      .row.g-5.text-center
        .col-md-6
          .card-container
            .card
              a.btn-custom(href="/medicos") GESTIONAR PROFESIONALES
              h5.card-title.mt-3 
             
          .card-description
            br
            p Administrar Profesionales y Especialidades.
            P Modifica, elimina o crea nuevos profesionales y especialidades en el sistema.
        .col-md-6
          .card-container
            .card
              a.btn-custom(href="/editAgenda") EDITAR/AGREGAR HORARIOS
             
              
              a.btn-custom(href="/create-agenda") CREAR NUEVA AGENDA

          .card-description
            
            br
            p Configura los horarios y agendas de los profesionales.


  script(src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js")
  script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js")





